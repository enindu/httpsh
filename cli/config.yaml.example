cert:
  ca:
    bits: 4096
    years: 1
    months: 0
    days: 0
    key: /path/to/ca/key.pem
    certificate: /path/to/ca/certificate.pem
  request:
    server:
      bits: 4096
      country:
        - TC
      organization:
        - Test Organization
      unit:
        - Test Unit
      locality:
        - Test Locality
      province:
        - Test Province
      domain: localhost
      years: 1
      months: 0
      days: 0
      key: /path/to/server/key.pem
      certificate: /path/to/server/certificate.pem
    client:
      bits: 4096
      country:
        - TC
      organization:
        - Test Organization
      unit:
        - Test Unit
      locality:
        - Test Locality
      province:
        - Test Province
      domain: localhost
      years: 1
      months: 0
      days: 0
      key: /path/to/client/key.pem
      certificate: /path/to/client/certificate.pem
server:
  network: tcp
  host: 127.0.0.1
  port: 5000
  domain: localhost
  read_timeout: 5
  write_timeout: 5
  idle_timeout: 60
  ca_certificate: /path/to/ca/certificate.pem
  server_key: /path/to/server/key.pem
  server_certificate: /path/to/server/certificate.pem
  directory: /home/enindu/Work/Go/Self/httpsh/
  mime: text/plain; charset=UTF-8
  methods:
    - GET
    - HEAD
  executables:
    cat:
      - --help
    grep:
      - --help
    ls:
      - --help
